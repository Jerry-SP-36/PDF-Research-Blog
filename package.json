{
  "name": "pdf-research-studio",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Local PDF Search research GUI powered by Codex App Server",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check lib/jobs.mjs && node --check lib/experience.mjs && node --check lib/reading-memory.mjs && node --check lib/report.mjs && node --check lib/workflow.mjs && node --check web/app.js"
  }
}
